@page
@model ST10058357_PROG7311_POE2.Pages.SubCategories.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductSubCategory[0].CategoryId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductSubCategory[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ProductSubCategory) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SubCategoryId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SubCategoryId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SubCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
